#include "ai/navigator/navigator.h"

std::map<unsigned int, Primitive> Navigator::getAssignedPrimitives(
    const std::vector<std::pair<unsigned int, Intent>> &assignedIntents,
    const World &world)
{
    // Unimplemented. Intended to be overridden.
}
